import React from 'react';
import SmartSchedulingDashboard from '@/components/scheduling/SmartSchedulingDashboard';

const SmartScheduling: React.FC = () => {
  return <SmartSchedulingDashboard />;
};

export default SmartScheduling;
